---
---
<section id="hero" class="hero">
  <div class="container hero-content">
    <div class="label reveal reveal-delay-1">DPS LABS — EST. 2019</div>
    
    <h1 class="title reveal reveal-delay-2">
      I ship <br />
      <span class="italic text-gold">products</span>
    </h1>
    
    <p class="description reveal reveal-delay-3">
      Full-stack engineer combining logical analysis with innovative solution delivery. 
      2x hackathon champion. 25,000+ users served.
    </p>
    
    <div class="cta-wrapper reveal reveal-delay-4">
      <a href="#work" class="btn btn-primary">
        EXPLORE MY WORK 
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7" />
        </svg>
      </a>
    </div>
  </div>

  <div class="scroll-indicator reveal reveal-delay-4">
    ↓ SCROLL TO EXPLORE
  </div>
</section>

<style>
  .hero {
    position: relative;
    height: 100vh;
    min-height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  /* Background effect (optional subtle grain or gradient) */
  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 50% 50%, rgba(212, 165, 116, 0.03) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    z-index: 1;
  }

  .label {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    color: var(--color-accent-gold);
    margin-bottom: var(--space-4);
  }

  .title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 8vw, 5rem);
    line-height: 1.1;
    font-weight: 700;
    margin-bottom: var(--space-6);
  }

  .italic {
    font-style: italic;
    font-weight: 400;
  }

  .description {
    font-size: 1.125rem;
    color: var(--color-ink-light);
    max-width: 480px;
    margin-bottom: var(--space-8);
    line-height: 1.6;
  }

  .cta-wrapper {
    margin-top: var(--space-2);
  }

  .scroll-indicator {
    position: absolute;
    bottom: var(--space-8);
    left: 50%;
    transform: translateX(-50%);
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    color: var(--color-ink-muted);
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateX(-50%) translateY(0);
    }
    40% {
      transform: translateX(-50%) translateY(-6px);
    }
    60% {
      transform: translateX(-50%) translateY(-3px);
    }
  }
</style>
